# Validation Rules for SimpleCP

## Project Configuration
- **Type:** Python Backend/API + Swift macOS Frontend
- **Language:** python, swift
- **Updated:** Nov 23, 2025

## File Size Limits
**Maximum file size:** 300 lines

```bash
# Check file sizes
find . -name "*.py" -o -name "*.js" -o -name "*.java" -o -name "*.go" -o -name "*.rs" -o -name "*.swift" | xargs wc -l | awk '$1 > 300 {print "VIOLATION: " $2 " has " $1 " lines (limit: 300)"}'
```

## Test Coverage Requirements
**Minimum coverage:** 90%

## Validation Tools
**Configured tools:** black,flake8,pytest,pyperclip

```bash
black --check .
flake8 --max-line-length=88 .
pytest --cov=. --cov-report=term-missing --cov-fail-under=90
```

## Custom Rules for Python Backend/API
- Functions should be under 50 lines
- Classes should be under 200 lines
- All public functions need docstrings

## Override Instructions
To modify these rules:
1. Edit this file directly
2. Run `git add docs/` and `git commit -m "Update validation rules"`
3. The AIs will use your updated rules immediately

---
**Auto-generated by Avery's AI Collaboration Hack**
**Customizable**: Feel free to modify these rules for your project needs
